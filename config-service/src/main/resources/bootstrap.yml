server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/JOSECONDORI5/api-store.git
          search-paths: config-data
          username: ${GIT_USER}
          passphrase: ${GIT_PASSWORD}
  security:
    user:
      name: root
      password: s3cr3t
